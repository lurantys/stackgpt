{
  "scripts": {
    "build": "npx terser extension/contentScript.js -o extension/contentScript.min.js -c passes=3 -m && cd extension && zip -r ../stackgpt-extension.zip . -x \"newicon.png\""
  },
  "devDependencies": {
    "terser": "^5.48.0"
  }
}
